# Список изменений (Changelog)

## Версия v7.1 (Официальный Мажорный Релиз)

### 🔴 Полный отказ от C++ STL (libstdc++.so):
* В `jni/Application.mk` задано `APP_STL := none`.
* Бинарник полностью пересобран под NDK r26b.
* **Проверка readelf -d показала:** Зависимость от `libstdc++.so` полностью исчезла! Теперь в `NEEDED` остались только системные нативные библиотеки: `liblog.so`, `libc.so`, `libm.so`, `libdl.so`. Демон запустится на 100% любых смартфонов и прошивок!

### 🔴 Исправлена защита PREROUTING TTL=1:
* Правило блокировки провокационных пакетов от вышки МТС перенесено на входящие сотовые интерфейсы модемов (`_celnt`): `iptables -t mangle -A PREROUTING -i "$_celnt" -m ttl --ttl-eq 1 -j DROP`
* Очистка в начале скрипта сбалансирована под `_celnt`.

### 🔴 Поддержка IPv6 Hop Limit=64 в режиме NFQUEUE:
* На устройствах без нативного таргета `HL` (как OnePlus 13) IPv6-трафик раздачи больше не дропается в `FORWARD DROP`, а направляется в `nfqttlo` (NFQUEUE), где C-демон безусловно выставляет `Hop Limit = 64`.

### 🟡 Безопасный запуск демона:
* Запуск утилиты в `service.sh` и `watchdog` избавлен от невалидных флагов `-s`/`-u` и переведен на классический `"$MODDIR/nfqttl" -d`.

### 🟡 Расширенный дамп в debug_log.sh:
* В секцию 6 добавлен дамп настройки `override_tether_enable_bpf_offload`.
* Исправлен вывод `dmesg` в секции 11.
