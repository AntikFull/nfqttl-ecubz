# Список изменений (Changelog)

## Версия v7.7 (Мажорный Релиз: Разблокировка Входящего Сотового Трафика Смартфона)

### 🔴 Главное Исправление:
* 💥 **Удалено гибельное правило `DROP TTL == 1` на входящем сотовом модеме (`_celnt`):**
  Анализ реального отладочного лога показал, что правило `iptables -t mangle -A PREROUTING -i rmnet+ -m ttl --ttl-eq 1 -j DROP` сбрасывало **523 полезных входящих пакета (282 Кбайта) ответов сайтов** от вышки оператора МТС, присылаемых промежуточными узлами связи с TTL=1!
  Удаление этого правила полностью разблокировало входящий сотовый интернет смартфона на 4G/5G!
