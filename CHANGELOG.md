# Список изменений (Changelog)

## Версия v5.6

* 🐛 **Авто-выдача прав 0755 на скрипты в installer:** Исправлена проблема, при которой распакованный из ZIP файла `debug_log.sh` не имел прав на исполнение. Теперь права 0755 ставятся автоматически для `debug_log.sh` и `action.sh` в `customize.sh`.
* 🛡 **Прямой запуск отладочного скрипта:** `service.sh` и `action.sh` вызывают `sh debug_log.sh` напрямую по наличию файла, гарантируя 100% срабатывание авто-отчета.

## Версия v5.5

* 🔍 **Логирование перехваченного/палящего трафика (Blocked Traffic Trace):** При включенном файле `debug` модуль логирует попытки подключений ПК/магнитол к заблокированным доменам и NTP-службам через `iptables LOG`.

## Версия v5.4

* 🛠 **Встроенный диагностический скрипт `debug_log.sh`:** Отладочный отчет со всеми системными деталями.

## Версия v5.3

* 📋 **Поддержка `blocklist.txt`:** Возможность добавлять любые домены/подстроки для блокировки.

## Версия v5.2

* 🚫 **Встроенная защита от «Анти-TTL МТС»:** Фильтрация Windows NCSI, системного времени и NTP (123 UDP).

## Версия v5.1

* 🧠 **Умный авто-выбор движка:** Kernel TTL vs Userspace NFQUEUE.
* 🛡 **Безопасная обработка IPv6:** Защита от утечки IPv6 TTL.

## Версия v5.0

* 🐛 Устранены SIGSEGV краши в C-коде.
* ⚡ Совместимость с KSU / APatch / Magisk.
