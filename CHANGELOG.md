# Список изменений (Changelog)

## Версия v5.4

* 🛠 **Встроенный диагностический скрипт `debug_log.sh`:** Добавлен скрипт для быстрого снятия отладочного отчета (`nfqttl_debug.log`) с деталями по модели устройства, ядру, состоянию `iptables` и демона `nfqttl`.
* ⚡ **Отладочный режим по флагу (Zero-overhead в релизе):** В обычной работе отладочный лог НЕ пишется и память не засоряется. Для автоматического формирования дебаг-лога при старте достаточно просто создать файл `debug` в папке модуля на смартфоне (`/data/adb/modules/nfqttl_ecubz/debug`).

## Версия v5.3

* 📋 **Поддержка пользовательского файла `blocklist.txt`:** Возможность добавлять любые домены/подстроки для блокировки детекции раздачи.

## Версия v5.2

* 🚫 **Встроенная защита от «Анти-TTL МТС» (Built-in Anti-MTS Host Protection):** Фильтрация Windows NCSI, системного времени и NTP (123 UDP).

## Версия v5.1

* 🧠 **Умный авто-выбор движка:** Kernel TTL vs Userspace NFQUEUE.
* 🛡 **Безопасная обработка IPv6:** Защита от утечки IPv6 TTL.
* ⚡ **Коррекция TCP MSS:** Автоматическая подгонка размера TCP окна.

## Версия v5.0

* 🐛 Устранены SIGSEGV краши и нехватки буфера в C-коде.
* ⚡ Совместимость с KSU / APatch / Magisk.
