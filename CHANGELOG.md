# Список изменений (Changelog)

## Версия v8.1-stealth (Устранение утечки 400 КБ при старте раздачи & Полная поддержка VPN/tun+)

### 🔴 Утечка 400 КБ при старте раздачи полностью ликвидирована:
* 🛡 **Глобальный щит Universal FORWARD TTL Shield:**
  В цепочку `FORWARD` самым первым правилом заведена мгновенная подмена `TTL=64` и `Hop Limit=64` для абсолютно ВСЕХ проходящих транзитных пакетов раздачи:
  `iptables -t mangle -I FORWARD 1 -j TTL --ttl-set 64`
  `ip6tables -t mangle -I FORWARD 1 -j HL --hl-set 64`
  Это исключает утечку первейших пакетов Windows/Android (DHCP, Router Solicitation), вылетавших в сеть в первейшую секунду включения точки доступа до отработки специфичных интерфейсных правил!
* 🚀 **Полная поддержка туннельных интерфейсов VPN (`tun+`):**
  Интерфейсы `tun+` (ByeDPI, Zapret, AdGuard, VPN) включены во все маски сотовых модемов `ALL_CELL_IFS`. Теперь трафик раздачи при включенных VPN-сервисах на телефоне фиксируется со 100% надежностью!
* 🔒 **Заглушка стартовых служб Windows:**
  В цепочку `FORWARD` добавлена блокировка специфичных локальных вещательных портов Windows при старте сети (mDNS 5353, SSDP 1900, LLMNR 5355, NetBIOS 137-138).
